# Supabase project settings → API Keys
PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
# Client-safe (sb_publishable_...). Used by SSR auth cookies for /studio.
PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key
# SERVER-ONLY (sb_secret_..., bypasses RLS). 
# Never import outside src/lib/server, *.server.ts, or scripts/.
SUPABASE_SECRET_KEY=your-secret-key
# Comma/space-separated Supabase Auth emails allowed into /studio.
STUDIO_ALLOWED_EMAILS=you@example.com
# Optional local-development-only bypass. Ignored by production builds even if set.
STUDIO_AUTH_BYPASS=false
# Ninja Kiwi Open Data API root.
NK_API_ROOT=https://data.ninjakiwi.com
# Canonical production origin used by public metadata and sitemap generation.
PUBLIC_SITE_URL=https://chimps.gg
